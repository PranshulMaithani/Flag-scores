# voxscore scoring machine requirements
#
#   pip install -r requirements.txt
#   python run_scoring.py
#
# torch: the default index gives CUDA wheels on Linux and Windows, which is what
# you want on the NVIDIA box. For a CPU-only machine use:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# and expect roughly 10-15x slower scoring. The GPU path needs about 4 GB VRAM.
#
# The spaCy English model is fetched automatically by run_scoring.py on first
# run, so it is not pinned here.

torch>=2.2
torchaudio>=2.2

transformers>=4.44
sentence-transformers>=3.0
huggingface_hub>=0.24
safetensors>=0.4
accelerate>=0.30
sentencepiece>=0.2
protobuf>=4.0

spacy>=3.7,<4
errant>=3.0
lexicalrichness>=0.5
wordfreq>=3.0
lingua-language-detector>=2.0

librosa>=0.10
soundfile>=0.12
soxr>=0.3

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
openpyxl>=3.1
